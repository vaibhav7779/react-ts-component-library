import { getFontsBasedOnType, BaseFontStyle } from './fonts.style';
import { COLORS } from './constants/colors';
import { FONTS, CHAR_SPACING, SPACING, WEIGHTS, LINE_HEIGHT } from './constants/dimensions';

export { getFontsBasedOnType, BaseFontStyle, FONTS, CHAR_SPACING, SPACING, WEIGHTS, LINE_HEIGHT };

export default COLORS;
